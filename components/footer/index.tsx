import React from 'react';
import Section from '../ui/features/Section';
import Container from '../ui/features/Container';

const Footer = () => {
    return (
        <Section className="py-20 border-t-[1px]">
            <Container>Footer</Container>
        </Section>
    );
};

export default Footer;
